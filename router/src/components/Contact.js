import React from "react";

// Note: the Home Component is in the form of a stateless functional component - as opposed to the class type components that we are more familiar with in class.

const Contact = () => {
    return(
        <div>
            <p>Contact</p>
        </div>
    );
};

export default Contact;